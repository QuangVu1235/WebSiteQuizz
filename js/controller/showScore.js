app.controller('showScore', function($rootScope, $http) {
  // alert($rootScope.student.fullname);
   $rootScope.student.fullname;
  console.log( $rootScope.student.fullname);
    // app.run(function ($rootScope, $http) {
    //   $http.get("http://localhost:3000/listKQ").then(function (response) {
    //       $rootScope.showscore = response.data;
    //       alert( $rootScope.showscore);
    //     });
    // });
});
